interface:
  display_name: "Expertise Compiler"
  short_description: "Use saved expertise to create, decide, learn and do work"
  default_prompt: "Use $expertise-compiler to apply these sources to my goal, improve my work, and save the collection and reusable method."
policy:
  allow_implicit_invocation: true
