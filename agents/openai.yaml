interface:
  display_name: "Expertise Compiler"
  short_description: "Turn transcripts into reusable, source-backed skills"
  default_prompt: "Use $expertise-compiler to compile my transcripts and suggest useful capabilities."
policy:
  allow_implicit_invocation: true
